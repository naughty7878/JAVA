spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test_jpa?allowPublicKeyRetrieval=true&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update #自动更新
    #日志中显示sql语句
    show-sql: true
